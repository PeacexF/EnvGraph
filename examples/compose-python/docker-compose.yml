services:
  web:
    build: ./app
    env_file:
      - .env
    environment:
      DB_HOST: ${POSTGRES_HOST}
      WORKERS: "4"

  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: app
