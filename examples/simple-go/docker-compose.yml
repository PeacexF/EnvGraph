services:
  api:
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL}
      REDIS_HOST: ${REDIS_HOST}
      PORT: ${PORT:-8080}
      LOG_LEVEL: info
    ports:
      - "8080:8080"

  worker:
    build: .
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - QUEUE_NAME=jobs
